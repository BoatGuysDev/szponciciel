# SQLAlchemy URL — required (no implicit default).
DATABASE_URL=sqlite:///szponciciel.db

# Application mode flag for non-DB behaviours (logging defaults, etc.).
RUN_MODE=development

# Log level: DEBUG | INFO | WARNING | ERROR.
LOG_LEVEL=INFO

# JSONL log file written in parallel with the live console output.
LOG_FILE=runs/logs/pipeline.jsonl

# Media root for stock video assets
# MEDIA_ROOT=media

# Voices root for voice cloning assets
# VOICES_ROOT=voices

# Compute device for TTS + WhisperX: cpu | cuda | mps
COMPUTE_DEVICE=cpu

# WhisperX model size for align_node: tiny | base | small | medium | large-v3
WHISPER_MODEL=base

# Coqui TTS model used by tts_node
TTS_MODEL=tts_models/multilingual/multi-dataset/xtts_v2

# Gemini model used by narrator + caption nodes
MODEL=gemini-2.5-flash-lite

# Google GenAI / Vertex auth (read directly by the Google SDK)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_GENAI_USE_VERTEXAI=true

# LangSmith tracing (optional) — read directly by the LangChain SDK.
# Set LANGSMITH_TRACING=true and provide a key to trace every run in LangSmith.
LANGSMITH_TRACING=false
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_API_KEY=your-api-key
LANGSMITH_PROJECT=szponciciel

# Zernio (TikTok upload) — read by the Zernio SDK
ZERNIO_API_KEY=your-api-key

# Tavily (article content fetching) — read by the Tavily SDK
TAVILY_API_KEY=your-api-key

# Required only when seeding the ground_truth_media persona
GROUND_TRUTH_MEDIA_ACCOUNT_ID=account-id

# Writer<->critic subgraph iteration limit (defaults to 3).
WRITER_CRITIC_MAX_ITERS=3

# Maximum character length for generated TikTok scripts (defaults to 8000).
MAX_SCRIPT_LENGTH=8000
